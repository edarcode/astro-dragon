---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const { class: cls, ...rest } = Astro.props;
---

<a {...rest} class:list={["surf", cls]}>Películas </a>

<style>
  .surf {
    padding: var(--sm) var(--md);
    border-radius: var(--sm);
    color: var(--blue);
    cursor: pointer;

    &:hover {
      color: var(--hover);
    }
  }
</style>
