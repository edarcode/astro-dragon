---
import css from "./Header.module.css";
import Logo from "../../../components/logos/Logo/Logo.astro";
import { ROUTER } from "../../../pages/rotuer";
---

<header class={css.header}>
  <nav class={css.nav}>
    <Logo />
    <div class={css.anchors}>
      <a href={ROUTER.index.path}>{ROUTER.index.display}</a>
      <a href={ROUTER.about.path}>{ROUTER.about.display}</a>
    </div>
  </nav>
</header>
